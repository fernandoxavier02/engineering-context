---
description: Generate the 4 mandatory always-loaded rules (.claude/rules/50–53) plus vault cards, adapted to the project.
---

Invoke the `engineering-context:generate-rules` skill. Requires PROJECT_PROFILE, ARCHITECTURE_MAP, and anti-pattern catalog — run the prerequisites if missing.
