---
description: Auto-detect language, framework, test framework, build tools and architecture style of the current project.
---

Invoke the `engineering-context:detect-stack` skill. Output the full PROJECT_PROFILE YAML.
