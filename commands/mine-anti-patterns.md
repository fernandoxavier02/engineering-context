---
description: Scan the codebase for anti-patterns (duplicated constants, silenced exceptions, venue leaks, etc.) and produce a catalog.
---

Invoke the `engineering-context:mine-anti-patterns` skill. Output the anti-pattern catalog with file:line references.
