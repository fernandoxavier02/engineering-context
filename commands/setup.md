---
description: Run the full 5-phase engineering context setup pipeline (detect stack → map architecture → mine anti-patterns → generate rules → validate).
---

Invoke the `engineering-context:setup` skill and run the complete pipeline end-to-end. Ask the user for confirmation at architecture-mapping phase as defined in the skill.
