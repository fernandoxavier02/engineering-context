---
description: Map project architecture — identify layers, dependency rules, mandatory flows. Requires detect-stack first (or runs it).
---

Invoke the `engineering-context:map-architecture` skill. If PROJECT_PROFILE is not available from a prior `detect-stack` run, run detect-stack first. Present the architecture diagram via AskUserQuestion for confirmation.
